#pragma once
#include <vector>
#include <set>
#include <iostream>
using namespace std;

void iteratorTest();

void replace(vector<string>& vec, string old, string replacement);

void replaceSet(set<string>& part, string old, string replacement);

void replaceImp(vector<string>& vec, string old, string replacement);